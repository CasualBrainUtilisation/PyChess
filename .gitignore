# ignore the pychache directory, as it is not needed for anyone to get this code to run

# following directories and file extensions are all used by replit and blocked as they are not needed to run the code too
src/__pycache__/
venv/
*.replit
*.replit.nix
*.toml
*.lock
*.nix
